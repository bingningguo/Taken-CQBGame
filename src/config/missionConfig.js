// Mission definitions are placeholders for later CQB objectives.
(function () {
  const TheGame = (window.TheGame = window.TheGame || {});

  TheGame.MissionConfig = {
    defaultMission: {
      id: "secure_villa",
      name: "Secure Villa",
      status: "In Progress",
    },
  };
})();
