Place your downloaded GLB map here.

Default expected file name:

```text
villa.glb
```

The loader reads this path from `src/config/gameConfig.js`.
Until `villa.glb` exists, the hand-built prototype level still runs normally.
